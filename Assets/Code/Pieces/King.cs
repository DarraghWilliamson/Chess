public class King : PieceObject {

    void Try(int x1, int y1) {
        //if (y1 >= 0 && y1 < 8 && x1 >= 0 && x1 < 8) {
        //    if (!board.squares[x1][y1].occupied) {
        //        board.squares[x1][y1].tile.ShowMoveable();
        //        activated.Add(board.squares[x1][y1].tile);
        //    } else {
        //        if (board.squares[x1][y1].peice != this) {
        //            if (white == board.squares[x1][y1].peice.white) {
        //                board.squares[x1][y1].tile.ShowBlocked();
        //                activated.Add(board.squares[x1][y1].tile);
        //            } else {
        //                board.squares[x1][y1].tile.ShowTakeable();
        //                activated.Add(board.squares[x1][y1].tile);
        //            }
        //        }

        //    }
        //}
    }

}
